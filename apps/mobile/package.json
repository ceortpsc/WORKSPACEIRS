{
  "name": "@ross/mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android",
    "build:web": "pnpm --filter @ross/web build",
    "release:prepare": "pnpm build:web && pnpm sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "typescript": "5.8.2"
  }
}
