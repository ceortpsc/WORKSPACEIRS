NEXT_PUBLIC_SITE_URL=https://www.rosstaxsoftware.com
APP_VERSION=0.1.0

IRS_GATEWAY_ENABLED=false
IRS_TDS_ENABLED=false
IRS_SOR_ENABLED=false
IRS_TIN_MATCHING_ENABLED=false
IRS_IRIS_ENABLED=false

# Store real credentials only in the deployment secret manager.
# Never commit client IDs, private keys, certificates, EFINs, CAF numbers, TCCs, passwords, or tokens.
